{
  "name": "shuhang-f-portfolio",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build && node scripts/validate-site.mjs",
    "check": "astro check",
    "validate": "node scripts/validate-site.mjs"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.10",
    "astro": "^7.2.2",
    "typescript": "^6.0.3"
  }
}
